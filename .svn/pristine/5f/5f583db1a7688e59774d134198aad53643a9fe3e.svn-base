using ServerAPI.ViewModels;
using System.Collections.Generic;

namespace ServerAPI.Services
{
    public interface IPersonService
    {
        List<PersonnelVm> GetPersonNameList(List<int> personnelId);
        PersonVm GetInmateDetails(int inmateId);
        PersonVm GetPersonDetails(int personId);
        bool IsPersonSealed(int personId);
    }
}
